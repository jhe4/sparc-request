%p= "Dear #{@send_to.try(:full_name)},"
%p.inline
  - if @action == "destroy"
    = t(:mailer)[:delete_intro]
  - if @action == "add"
    = t(:mailer)[:add_intro]
  - if @action == "update"
    = t(:mailer)[:update]
%p.inline
  %a.link{:href => @protocol_link}=t(:mailer)[:sparc_dashboard_period]